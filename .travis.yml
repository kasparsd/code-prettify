dist: xenial

language: php

php: '5.6'

install:
  - composer install

script:
  - composer lint

# Travis must run on brances that need to be deployed.
branches:
  only:
  - master
  - develop

cache:
  npm: true
  directories:
    - $HOME/.composer/cache

notifications:
  email: false
